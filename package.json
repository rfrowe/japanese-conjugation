{
	"dependencies": {
		"wanakana": "^5.0.2"
	},
	"devDependencies": {
		"@parcel/packager-raw-url": "^2.12.0",
		"@parcel/transformer-webmanifest": "^2.12.0",
		"parcel": "^2.12.0"
	},
	"source": "src/index.html",
	"scripts": {
		"dev": "COMMIT_HASH=$(git rev-parse --short HEAD) parcel --dist-dir ./hot-reload-temp",
		"build": "COMMIT_HASH=$(git rev-parse --short HEAD) parcel build --dist-dir ./dist --no-source-maps --public-url ./"
	}
}
